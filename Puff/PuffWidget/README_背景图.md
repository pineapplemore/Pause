# 小组件背景图放置说明

在 Xcode 中为小组件设置自定义背景图：

1. 打开 **PuffWidget/Assets.xcassets**
2. 选中 **WidgetBackground** 的 1x（或 2x/3x）槽位
3. 将你的背景图拖入该 imageset，或在该槽位填写图片文件名（图片需放在同目录 `WidgetBackground.imageset/` 下）

若不需要背景图，不放入任何图片即可，小组件会使用默认透明背景。
